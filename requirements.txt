# YOLOv8 Object Detection - Core Dependencies
# Install with: pip install -r requirements.txt

# Core ML/CV libraries
ultralytics>=8.3.0      # YOLOv8 object detection
opencv-python>=4.12.0   # Computer vision and image processing
torch>=2.9.0            # PyTorch deep learning framework
pyyaml>=6.0             # YAML configuration file support

# Note: PyTorch will automatically install CUDA dependencies if GPU is available
# For CPU-only installation, use: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
